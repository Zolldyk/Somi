[profile.default]
src = "src"
out = "out"
libs = ["lib"]
test = "test"
script = "script"
solc = "0.8.30"
evm_version = "cancun"
optimizer = true
optimizer_runs = 200

[fuzz]
runs = 256
depth = 64

[rpc_endpoints]
somnia_testnet = "${SOMNIA_TESTNET_RPC}"
